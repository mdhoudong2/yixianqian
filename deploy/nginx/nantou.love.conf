# app.nantou.love - H5网页版
server {
    listen 443 ssl http2;
    listen [::]:443 ssl;
    server_name app.nantou.love;

    ssl_certificate /etc/letsencrypt/live/app.nantou.love/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/app.nantou.love/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    # HSTS：强制浏览器走 HTTPS
    add_header Strict-Transport-Security "max-age=31536000" always;

    client_max_body_size 20M;

    # 引流埋点限流：同 IP 5次/秒，突发 10
    location /api/track {
        limit_req zone=track burst=10 nodelay;
        proxy_pass http://127.0.0.1:8091;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 公开页已迁移至 nantou.love 根路径
    location = /public.html {
        return 301 https://nantou.love/;
    }

    location / {
        proxy_pass http://127.0.0.1:8091;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_buffering off;
    }
}

server {
    listen 80;
    listen [::]:80;
    server_name app.nantou.love;

    location /.well-known/acme-challenge/ {
        root /var/www/nantou.love;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# nantou.love - 公开页（生产环境，proxy -> 8091 main 后端）
server {
    listen 443 ssl http2;
    listen [::]:443 ssl;
    server_name nantou.love www.nantou.love;

    ssl_certificate /etc/letsencrypt/live/nantou.love/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/nantou.love/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    # HSTS：强制浏览器走 HTTPS
    add_header Strict-Transport-Security "max-age=31536000" always;

    # 公开页只在根路径，移除 /public.html 直接访问
    location = /public.html {
        return 301 https://nantou.love/;
    }

    # 根路径直接渲染公开页（由 Flask 注入生产环境二维码 qrcode.png）
    location = / {
        proxy_pass http://127.0.0.1:8091/public.html;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 引流埋点限流：同 IP 5次/秒，突发 10
    location /api/track {
        limit_req zone=track burst=10 nodelay;
        proxy_pass http://127.0.0.1:8091;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 其余走生产后端：lib/ 静态资源、qrcode、/api/*
    location / {
        proxy_pass http://127.0.0.1:8091;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_buffering off;
    }
}

server {
    listen 80;
    listen [::]:80;
    server_name nantou.love www.nantou.love;

    location /.well-known/acme-challenge/ {
        root /var/www/nantou.love;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
