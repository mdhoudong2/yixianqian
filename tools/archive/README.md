# 归档脚本（一次性/历史迁移脚本）

本目录存放一次性运维/迁移/测试脚本，已从仓库根目录归档至此，**不参与线上运行**。

- `backfill_*` / `fix_*` / `add_*` / `setup_*`：历史数据迁移、补字段、造测试数据。
- `check_*` / `test_*` / `simulate_*`：临时排查、联调用的只读脚本。

> 这些脚本大多硬编码了表 ID（非密钥），仅作参考保留。密钥仍统一放 `local_config.py`（已 gitignore），不要写进脚本。
